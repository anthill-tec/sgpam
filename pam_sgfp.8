.TH PAM_SGFP 8 "2026-03-04" "sgpam 0.1.0" "System Administration"
.SH NAME
pam_sgfp \- PAM module for SecuGen fingerprint authentication
.SH SYNOPSIS
.B auth
.RI [ sufficient | required ]
.B pam_sgfp.so
.RI [ brightness_step = N ]
.RI [ retries = N ]
.SH DESCRIPTION
.B pam_sgfp
is a PAM authentication module that captures a live fingerprint from
a SecuGen U20 reader and matches it against templates previously
enrolled with
.BR sg_enroll (1).
.PP
The module supports multi-finger enrollment: all templates for a user
are loaded and tried in sequence.
Authentication succeeds on the first matching template.
.PP
Templates are discovered by glob pattern:
.IR /etc/security/sg_fingerprints/ username _*.tpl
(finger-specific templates) and
.IR /etc/security/sg_fingerprints/ username .tpl
(legacy single-finger template).
Both formats are supported for backward compatibility.
.PP
If no template exists for the authenticating user, the module returns
.B PAM_USER_UNKNOWN
so that a subsequent PAM rule (typically password authentication) can
take over.
This enables a graceful fallback: enrolled users authenticate by
fingerprint, while unenrolled users are silently passed through to
password.
.PP
The module prompts via the PAM conversation function with the names
of enrolled fingers, for example:
.RB \(lq "Place finger on scanner (right-index, left-thumb)..." \(rq.
For legacy single-finger templates the generic prompt
.RB \(lq "Place finger on scanner..." \(rq
is shown instead.
Capture uses a 10-second timeout per attempt.
The module samples the sensor silently up to three times (see
.BR retries ),
accepting authentication on the first sample that matches and rejecting
only after every attempt has failed.
This makes a single prompt tolerant of a hurried or partial first finger
placement, without asking the user to scan again.
Matching uses security level
.B SL_NORMAL
(match score >= 80/199).
.SH MODULE ARGUMENTS
.TP
.BI brightness_step= N
Control how the sensor exposure (LED brightness) escalates across the retry
attempts.
There is no way to set an absolute brightness: the module reads the sensor's
current LED brightness from the device on each authentication and uses it as
the escalation
.I floor
(falling back to 50 only if the readout is unusable).
The floor is logged via
.BR syslog (3)
so the active range is visible.
.IP
By default (argument absent) an
.B adaptive ladder
is used: the brightness scales evenly from the floor up to the ceiling of
100, spread across the
.B retries
attempts.
The ceiling is hard-clamped at 100 (the SDK does not police out-of-range
values itself).
The first attempt always keeps the sensor's own exposure untouched and
scaling begins on the second attempt, so a sensor that already works is
left alone.
.IP
A positive
.I N
overrides the adaptive ladder with a fixed increment of
.I N
added per attempt (capped at 100).
.B brightness_step=0
disables escalation entirely (constant exposure).
.IP
For example, a sensor reporting 40 with
.B retries=3
samples at (sensor), 70, 100; reporting 70 with
.B retries=4
gives (sensor), 80, 90, 100; while
.B "brightness_step=20 retries=3"
from a floor of 40 forces (sensor), 60, 80.
Values outside 0\(en100 are ignored and logged.
.TP
.BI retries= N
Number of times to silently sample the sensor before rejecting,
an integer from 1 to 10 (default 3).
Each attempt captures, extracts and matches; authentication succeeds on
the first matching sample, and the module rejects only when all
.I N
attempts have failed.
Raising this gives the user more time to settle a finger within a single
prompt; lowering it to 1 restores the original single-shot behaviour.
Note the 10-second capture timeout is
.B per attempt,
so the worst-case wait with no finger present is
.IR N " \(mu 10"
seconds.
Values outside 1\(en10 are ignored and logged.
.SH MODULE TYPES PROVIDED
.B auth
.SH RETURN VALUES
.TP
.B PAM_SUCCESS
The live fingerprint matched an enrolled template.
.TP
.B PAM_AUTH_ERR
Authentication failed.
This covers: match failure against all templates, capture timeout,
SDK initialisation errors, device not found, or invalid username.
.TP
.B PAM_USER_UNKNOWN
No enrolled template was found for the user.
When
.B pam_sgfp.so
is configured as
.BR sufficient ,
PAM skips it and tries the next module in the stack.
.SH FILES
.TP
.I /usr/lib/security/pam_sgfp.so
The shared library installed by the package.
.TP
.I /etc/security/sg_fingerprints/
Directory containing per-user fingerprint templates (mode 0700, root only).
.TP
.IR /etc/security/sg_fingerprints/ username _ finger-name .tpl
Enrolled template for a specific finger (e.g.,
.IR alice_right-index.tpl ).
Mode 0600.
.TP
.IR /etc/security/sg_fingerprints/ username .tpl
Legacy single-finger template.
Still recognised for backward compatibility.
.SH SECURITY
.TP
.B Match threshold
Uses
.B SL_NORMAL
security level, requiring a match score of at least 80 out of 199.
.TP
.B Template format
Templates use SecuGen SG400 format with AES encryption.
.TP
.B Capture quality floor
Images scoring below 40/100 quality are rejected before matching.
.TP
.B Path traversal protection
Usernames are validated against a strict character set before
constructing the template file path.
Slashes and special characters are rejected.
.TP
.B Image dimension guard
Abnormal image dimensions (0 or >4096 in either axis) are rejected
to prevent buffer overflow.
.SH DIAGNOSTICS
All messages are logged via
.BR syslog (3)
with facility
.BR LOG_AUTH .
.TP
.B "pam_sgfp: fingerprint accepted for user \(aq%s\(aq (template %d/%d)"
.RB ( LOG_INFO )
Authentication succeeded.
The template index indicates which enrolled finger matched.
.TP
.B "pam_sgfp: fingerprint rejected for user \(aq%s\(aq (%d templates tried)"
.RB ( LOG_NOTICE )
The live fingerprint did not match any enrolled template.
.TP
.B "pam_sgfp: no enrolled fingerprint for user \(aq%s\(aq"
.RB ( LOG_NOTICE )
No template file found; module returns
.BR PAM_USER_UNKNOWN .
.TP
.B "pam_sgfp: capture failed for \(aq%s\(aq"
.RB ( LOG_NOTICE )
The scanner timed out or the finger was removed before capture completed.
.TP
.B "pam_sgfp: SGFPM_OpenDevice failed \(em scanner connected?"
.RB ( LOG_ERR )
The SecuGen U20 reader was not detected on any USB port.
.TP
.B "pam_sgfp: invalid username rejected (path traversal?)"
.RB ( LOG_WARNING )
The PAM username contained disallowed characters.
.SH EXAMPLES
.SS sudo authentication
Add to
.I /etc/pam.d/sudo
before the existing password rule:
.PP
.RS
.nf
auth  sufficient  pam_sgfp.so
auth  include     system-auth
.fi
.RE
.PP
Allowing more samples and a fixed exposure step before falling through to
the password rule:
.PP
.RS
.nf
auth  sufficient  pam_sgfp.so retries=4 brightness_step=20
auth  include     system-auth
.fi
.RE
.SS Display manager (SDDM)
Add to
.IR /etc/pam.d/sddm :
.PP
.RS
.nf
auth  sufficient  pam_sgfp.so
auth  include     system-login
.fi
.RE
.SS Console login
Add to
.IR /etc/pam.d/login :
.PP
.RS
.nf
auth  sufficient  pam_sgfp.so
auth  include     system-local-login
.fi
.RE
.SH SEE ALSO
.BR sg_enroll (1),
.BR pam (8),
.BR pam.d (5),
.BR pam.conf (5)
